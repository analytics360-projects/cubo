apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "amaterasu-project.fullname" . }}-test-connection"
  labels:
    {{- include "amaterasu-project.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "amaterasu-project.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
