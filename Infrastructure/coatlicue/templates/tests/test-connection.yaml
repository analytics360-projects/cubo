apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "coatlicue-project.fullname" . }}-test-connection"
  labels:
    {{- include "coatlicue-project.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "coatlicue-project.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
