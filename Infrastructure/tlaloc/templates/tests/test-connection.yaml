apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Tlaloc.Api.fullname" . }}-test-connection"
  labels:
    {{- include "Tlaloc.Api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Tlaloc.Api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
