apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "anubis.Api.fullname" . }}-test-connection"
  labels:
    {{- include "anubis.Api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "anubis.Api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
